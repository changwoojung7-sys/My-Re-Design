// App Version Control
// Update this version string to force a global logout and cache clear for all users.
// Useful for ensuring users fetch new permissions/tokens after a deployment.
export const APP_VERSION = 'v1.0.2';
